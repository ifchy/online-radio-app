---
phase: 01-playback-engine-walking-skeleton
plan: 12
type: execute
wave: 7
depends_on: ["01-10"]
files_modified:
  - lib/features/playback/engine/ports.dart
  - lib/features/playback/engine/connectivity_port_impl.dart
  - lib/features/playback/engine/state_machine.dart
  - lib/features/playback/engine/radio_audio_handler.dart
  - lib/app/bootstrap.dart
  - test/support/fakes.dart
  - test/features/playback/engine/connectivity_port_impl_test.dart
  - test/features/playback/engine/state_machine_test.dart
  - test/features/playback/engine/radio_audio_handler_test.dart
  - test/app/tracer_e2e_test.dart
autonomous: true
requirements: [PLAY-07, PLAY-10]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "After a Wi-Fi to 4G switch or an airplane-mode toggle, audio comes back at the live edge within about 10 s of connectivity returning, without user action"
    - "While offline the app waits for the network instead of burning retries, and after 10 minutes offline it gives up: the foreground service stops and the notification offers Play"
    - "A network change while playing is checked after 5 s, and the stream is reloaded only if audio stopped arriving"
    - "Two reconnect triggers for the same outage (for example the stall watchdog and a network change in the same moment) start exactly one fresh connection, and an 'online' event while already connecting does not restart it"
    - "Connectivity events after a user pause or stop start nothing"
  artifacts:
    - path: "lib/features/playback/engine/connectivity_port_impl.dart"
      provides: "connectivity_plus adapter, debounced, online + networkChanged"
      contains: "onConnectivityChanged"
    - path: "lib/features/playback/engine/ports.dart"
      provides: "ConnectivityPort and ConnectivityChange"
      contains: "ConnectivityPort"
    - path: "lib/features/playback/engine/state_machine.dart"
      provides: "Connectivity, flow-check and offline-budget rows"
      contains: "ConnectivityChanged"
  key_links:
    - from: "lib/app/bootstrap.dart"
      to: "lib/features/playback/engine/connectivity_port_impl.dart"
      via: "ConnectivityPortImpl(Connectivity()) passed to RadioAudioHandler"
      pattern: "ConnectivityPortImpl\\("
    - from: "lib/features/playback/engine/radio_audio_handler.dart"
      to: "lib/features/playback/engine/state_machine.dart"
      via: "ConnectivityPort.changes and StreamPlayer.bufferedPositions become ConnectivityChanged and BufferedPosition events on the serial queue"
      pattern: "ConnectivityChanged"
---

# Phase 1 Plan 12: Wi-Fi↔4G switches and offline periods are ridden out without draining the battery

## Phase Goal

ROADMAP Phase 1 Goal (verbatim): "A user on a physical Android phone can start a Bulgarian station and it keeps playing on its own through screen-off, background, calls, headphone changes, network switches and dead primary streams. The live edge, correct Cyrillic now-playing and media controls all work." (This line is not in user-story form; see 01-01-PLAN.md "Phase Goal".)

<objective>
Feed network state into the state machine. Reconnect at once when the network returns or changes, instead of waiting for ExoPlayer's I/O timeout. Wait without retrying while offline, and give up after the offline budget (D-10). Check the flow after a network change while playing.

Purpose: the Wi-Fi ↔ mobile data clause of PLAY-07 and SC3 (airplane toggle, Wi-Fi→4G), the offline half of the D-10 budget built in 01-10, and PLAY-10 for connectivity events.
Output: `ConnectivityPort` and its connectivity_plus adapter, the connectivity, flow-check and offline-budget rows, the handler and bootstrap wiring, and tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-playback-engine-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-playback-engine-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-playback-engine-walking-skeleton/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From 01-01, 01-09 and 01-10; read the real files:
- `ports.dart`: `StreamPlayer.bufferedPositions` (generation, Duration position)
- `state_machine.dart`: `EngineState` (…, budget: RetryBudgetClock, attempt), `TimerKind` (connect, stall, backoff, stablePlaying, budget), `EngineTimings`, the Reconnecting and budget rows
- `reconnect_policy.dart`: `RetryBudgetClock.onConnectivity(DateTime now, bool online)`, `exhausted(now)` returning offline or online
- `radio_audio_handler.dart`: constructor (StreamPlayer, AudioSessionPort, StationDirectory, StreamResolver, EngineStrings, {initialRetryBudget}), serial event queue
- `test/app/tracer_e2e_test.dart` and `radio_audio_handler_test.dart` construct RadioAudioHandler directly
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A debounced connectivity adapter reports when the network goes away, comes back or changes</name>
  <files>lib/features/playback/engine/ports.dart, lib/features/playback/engine/connectivity_port_impl.dart, test/support/fakes.dart, test/features/playback/engine/connectivity_port_impl_test.dart</files>
  <read_first>lib/features/playback/engine/ports.dart, test/support/fakes.dart, .planning/phases/01-playback-engine-walking-skeleton/01-RESEARCH.md (transitions 346-347, connectivity_plus note line 513), .planning/phases/01-playback-engine-walking-skeleton/COVERAGE.md (connectivity_plus rows)</read_first>
  <behavior>
    - ConnectivityPortImpl over a fake Connectivity: [wifi] then [mobile] after the debounce → one ConnectivityChange(online: true, networkChanged: true); [wifi] then [none] → online false; a burst of three changes within 500 ms → one event; isOnline() is false for [none] and true for [mobile].
    - FakeConnectivityPort reports a settable online value and emits scripted changes.
  </behavior>
  <action>
0. Preflight: if `flutter --version` does not report Flutter 3.47.5, install it as in 01-01-PLAN.md Task 1 step 0.

1. ports.dart: add `abstract interface class ConnectivityPort` with `Stream<ConnectivityChange> get changes` and `Future<bool> isOnline()`, where `ConnectivityChange(bool online, bool networkChanged)`.

2. lib/features/playback/engine/connectivity_port_impl.dart: map `Connectivity().onConnectivityChanged` (a `List<ConnectivityResult>`, from the default-network callback on API 24+) with a 500 ms debounce.
   - online = the list contains a result other than none.
   - networkChanged = the set of results differs from the previous non-empty set.
   - isOnline() uses `Connectivity().checkConnectivity()` with the same online rule (COVERAGE.md lists both connectivity_plus capabilities as INTEGRATE).
   - Take the Connectivity instance and the debounce duration as constructor parameters so tests can inject a fake.

3. test/support/fakes.dart: add FakeConnectivityPort (settable online, an emit helper for changes).

4. Tests first: test/features/playback/engine/connectivity_port_impl_test.dart for the behaviour list, using a fake Connectivity under fakeAsync.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze && dart analyze && flutter test test/features/playback/engine/connectivity_port_impl_test.dart</automated>
    <fails_when>non-zero exit from any step, or "All tests passed!" missing</fails_when>
    <automated>grep -q 'onConnectivityChanged' lib/features/playback/engine/connectivity_port_impl.dart && grep -q 'checkConnectivity' lib/features/playback/engine/connectivity_port_impl.dart && grep -q 'ConnectivityPort' lib/features/playback/engine/ports.dart && grep -q 'class FakeConnectivityPort' test/support/fakes.dart</automated>
    <fails_when>non-zero exit: the port, the connectivity adapter or the fake is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - connectivity_port_impl_test.dart passes: debounce, online rule and networkChanged rule; isOnline for none and mobile.
    - FakeConnectivityPort exists in test/support/fakes.dart.
  </acceptance_criteria>
  <done>A debounced ConnectivityPort exists, backed by the one adapter allowed to import connectivity_plus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wi-Fi↔4G switches and airplane toggles come back at the live edge, and offline waits end after the budget</name>
  <files>lib/features/playback/engine/state_machine.dart, lib/features/playback/engine/radio_audio_handler.dart, lib/app/bootstrap.dart, test/features/playback/engine/state_machine_test.dart, test/features/playback/engine/radio_audio_handler_test.dart, test/app/tracer_e2e_test.dart</files>
  <read_first>lib/features/playback/engine/state_machine.dart, lib/features/playback/engine/reconnect_policy.dart, lib/features/playback/engine/radio_audio_handler.dart, lib/app/bootstrap.dart, test/features/playback/engine/state_machine_test.dart, test/app/tracer_e2e_test.dart, .planning/phases/01-playback-engine-walking-skeleton/01-CONTEXT.md (D-10), .planning/phases/01-playback-engine-walking-skeleton/01-RESEARCH.md (transitions 341-356, RetryBudget accounting 365-368, Assumption A13), .planning/research/ARCHITECTURE.md (Pattern 4 lines 283-304), .planning/research/PITFALLS.md (Pitfall 3 lines 65-84, Pitfall 4 lines 88-105)</read_first>
  <behavior>
    - The handler seeds the initial online state from isOnline() and puts every ConnectivityChange on the serial queue as ConnectivityChanged(online, networkChanged), and every buffered-position event as BufferedPosition(generation, position).
    - The tracer test still passes with a FakeConnectivityPort that reports online.
    - Connectivity offline during an outage → Reconnecting(waitingForNetwork: true): no backoff timer, and the offline budget clock runs. Online or a network change while Reconnecting or Buffering → Connecting immediately, with the attempt reset and a fresh load at the live edge.
    - A network change while Playing → a 5 s flow check. If bufferedPosition has not advanced when it fires → immediate reload. If it advanced → nothing happens.
    - RetryBudget standard: 10 min offline → PlaybackError(offline) with StopTransport, ReleaseFocus, all timers cancelled and playing false. A budget timer for the remaining offline time ends the wait even though no backoff timer runs.
    - Idempotency: a stall-timer fire and a network-change event in the same fakeAsync tick produce exactly one Load. An 'online' event while Connecting produces no new Load.
    - Concurrency: in Paused, Idle or PlaybackError, ConnectivityChanged(online) and network changes produce no Load (PLAY-10); UserPause during a waitingForNetwork Reconnecting cancels the budget timer.
  </behavior>
  <action>
1. state_machine.dart:
   - New events: ConnectivityChanged(online, networkChanged) and BufferedPosition(generation, position). TimerKind gains flowCheck. EngineTimings gains flowCheckDelay 5 s and connectivityDebounce 500 ms (Claude's discretion; tune on a device, A13). EngineState gains `bool online` and the last buffered position for the flow check.
   - Add the connectivity, flow-check and offline-budget rows from the behaviour list, per RESEARCH 341-356 and ARCHITECTURE Pattern 4. Update the budget clock with RetryBudgetClock.onConnectivity on every ConnectivityChanged, and arm the budget timer for the remaining time of the current connectivity class whenever an outage is active.
   - On offline exhaustion: StopTransport, ReleaseFocus, CancelAllTimers, then PlaybackError(offline).
   - Every reload increments the generation, so a second trigger in the same outage finds the state already Connecting and emits no Load (idempotency).
2. radio_audio_handler.dart: take a ConnectivityPort in the constructor. Seed the online state with isOnline(), and put ConnectivityChanged (from ConnectivityPort.changes) and BufferedPosition (from StreamPlayer.bufferedPositions) on the serial queue.
3. lib/app/bootstrap.dart: pass ConnectivityPortImpl(Connectivity()) to RadioAudioHandler.
4. Tests first: extend state_machine_test.dart (pure rows) and radio_audio_handler_test.dart (fakeAsync, constructing the handler with a FakeConnectivityPort) with every case in the behaviour list, including the 10 min offline budget, the flow check with and without progress, the idempotency case and the paused/stopped no-restart cases. Update test/app/tracer_e2e_test.dart to pass a FakeConnectivityPort that reports online; all of its assertions must still hold. All earlier tests must keep passing.
  </action>
  <verify>
    <automated>flutter pub get && dart run build_runner build -d && flutter analyze && dart analyze && flutter test</automated>
    <fails_when>non-zero exit from any step, or "All tests passed!" missing</fails_when>
    <automated>grep -q 'ConnectivityChanged' lib/features/playback/engine/state_machine.dart && grep -q 'flowCheckDelay' lib/features/playback/engine/state_machine.dart && grep -q 'waitingForNetwork' lib/features/playback/engine/state_machine.dart && grep -q 'ConnectivityChanged' lib/features/playback/engine/radio_audio_handler.dart && grep -q 'ConnectivityPortImpl(' lib/app/bootstrap.dart && ! grep -qE "^import 'package:(flutter|just_audio|audio_service|audio_session|connectivity_plus)/" lib/features/playback/engine/state_machine.dart</automated>
    <fails_when>non-zero exit: the connectivity rows, the flow check, the offline wait, the handler or bootstrap wiring is missing, or the reducer imports Flutter or plugin packages</fails_when>
    <human-check>
      <test>Owner, on Xiaomi and Samsung in a release build (the debug panel from 01-11 helps in a debug build): (1) play a station on Wi-Fi, then `adb shell svc wifi disable`; (2) `adb shell cmd connectivity airplane-mode enable`, wait 60 s, then disable it; (3) pause, toggle Wi-Fi off and on, and wait 2 minutes; (4) play a station, enable airplane mode and wait more than 10 minutes.</test>
      <expected>(1)-(2) Audio returns by itself within about 10 s of connectivity returning, at the live edge. The notification stays up showing "Повторно свързване…" meanwhile. (3) Nothing restarts. (4) After the offline budget runs out the notification shows the error state with a Play button, and `adb shell dumpsys activity services bg.izk.radio` shows no foreground service (SC3).</expected>
      <why_human>Real network switches, airplane mode and OS FGS behaviour cannot be simulated in the container.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The handler/state-machine tests pass these cases: offline → waitingForNetwork with no backoff; online → immediate retry at the live edge; network change while Playing → flow check → reload only without progress; 10 min offline → PlaybackError(offline) with release and playing false; one Load per double trigger; online while Connecting → no new Load; no Load from connectivity events in Paused, Idle or PlaybackError.
    - bootstrap passes `ConnectivityPortImpl(`, and the tracer test passes with a FakeConnectivityPort.
    - state_machine.dart still imports no Flutter or plugin package.
    - The full suite is green, and `flutter analyze` and `dart analyze` are clean.
  </acceptance_criteria>
  <done>Playback rides out network switches and offline periods at the live edge, waits instead of retrying while offline, gives up after the D-10 offline budget, and never restarts after a user pause or stop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network / OS connectivity events → engine | Noisy, possibly flapping signals drive retries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Denial of Service (battery and data drain) | retries while offline | high | mitigate | No backoff retries while offline (waitingForNetwork), the offline budget (standard 10 min) with a budget timer, and full release (FGS, focus, timers) on exhaustion; unit-tested with fakeAsync. |
| T-12-02 | Denial of Service | connectivity event storms (flapping networks) | low | mitigate | A 500 ms debounce in ConnectivityPortImpl; an 'online' event while Connecting is a no-op, and a double trigger gives one Load (idempotency tests). |
| T-12-03 | Tampering (unwanted playback) | connectivity events after a user pause or stop | medium | mitigate | Connectivity rows exist only for Reconnecting, Buffering and Playing; Paused, Idle and PlaybackError ignore them; unit-tested (PLAY-10). |
</threat_model>

<verification>
- `flutter pub get && dart run build_runner build -d && flutter analyze && dart analyze && flutter test` passes.
- The grep gates for the adapter, its wiring, the connectivity rows and reducer purity pass.
</verification>

<success_criteria>
- SC3 (network clause): live-edge reconnect within ~10 s after a Wi-Fi→4G switch or an airplane toggle; offline waits end after the budget.
- PLAY-10: connectivity never restarts user-paused or user-stopped playback.
- D-10: the offline half of the standard budget is live.
</success_criteria>

## Edge Coverage (spec-less fallback — items owned by this plan)

| Req | Category | Status | Lifted as |
|-----|----------|--------|-----------|
| PLAY-07 | idempotency | resolved (explicit) | truth: two triggers in one outage give one fresh connection; online while connecting does not restart |

## Artifacts this phase produces (this plan)

- Types: `ConnectivityPort`, `ConnectivityChange`, `ConnectivityPortImpl`.
- State machine additions: events `ConnectivityChanged`, `BufferedPosition`; `TimerKind.flowCheck`; `EngineTimings.flowCheckDelay` 5 s, `connectivityDebounce` 500 ms; `EngineState.online`.
- Handler: constructor parameter `ConnectivityPort`.
- Test support: `FakeConnectivityPort`; test `test/features/playback/engine/connectivity_port_impl_test.dart`.

<output>
Create `.planning/phases/01-playback-engine-walking-skeleton/01-12-SUMMARY.md` when done.
</output>
