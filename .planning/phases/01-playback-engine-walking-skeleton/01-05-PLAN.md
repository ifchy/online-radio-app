---
phase: 01-playback-engine-walking-skeleton
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/features/catalog/data/phase1_stations.dart
  - lib/features/catalog/data/debug_stations.dart
  - lib/features/catalog/data/station_directory.dart
  - test/features/catalog/phase1_stations_test.dart
  - test/features/catalog/station_test.dart
autonomous: false
requirements: [STRM-01, STRM-02, STRM-03, STRM-04, STRM-05, PLAY-08]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The station list offers the six owner-chosen Bulgarian stations (БНР Хоризонт, Радио 1, БГ Радио, Energy, N-JOY, Радио Витоша), each with only owner-verified official streams, unless the owner explicitly recorded that a station has no public official stream"
    - "Each station carries its real alternate endpoints as an ordered streams[] list (primary first, then fallbacks)"
    - "Across the release list the streams cover MP3, AAC/HE-AAC, HLS, a .pls or .m3u wrapper and a plain http:// stream; the non-.m3u8 HLS and windows-1251 cases are covered as the owner decided at the D-02 checkpoint"
    - "In debug and profile builds the list also shows test stations whose primary stream is deliberately dead (and one deliberately slow), with a real fallback behind it"
    - "The release station list contains no debug test station"
  prohibitions:
    - statement: "MUST NOT ship any stream endpoint that is not the station's own official, publicly offered stream (no aggregator, relay or re-streamer URL), and MUST NOT silently substitute a different station or source to fill a missing format (D-03)"
      status: resolved
      verification: judgment
  artifacts:
    - path: "lib/features/catalog/data/phase1_stations.dart"
      provides: "The owner-verified six-station release list with ordered streams[] (D-01..D-04)"
      contains: "curated('bnr-horizont')"
    - path: "lib/features/catalog/data/debug_stations.dart"
      provides: "Debug-only dead-primary, slow-primary and HLS-sniff test stations (D-05)"
      contains: "StationId.debug"
    - path: "lib/features/catalog/data/station_directory.dart"
      provides: "Release list plus debug stations only when !kReleaseMode"
      contains: "kReleaseMode"
    - path: "test/features/catalog/phase1_stations_test.dart"
      provides: "Release list has no debug ids; the D-03 format coverage matrix holds"
      min_lines: 30
    - path: "test/features/catalog/station_test.dart"
      provides: "StationId parse/format round trip and the non-empty streams[] invariant (assumption-delta decision in 01-01)"
      min_lines: 20
  key_links:
    - from: "lib/features/catalog/data/station_directory.dart"
      to: "lib/features/catalog/data/debug_stations.dart"
      via: "StationDirectory.phase1({bool includeDebug = !kReleaseMode}) appends debugStations only when includeDebug"
      pattern: "includeDebug"
    - from: "lib/features/catalog/data/debug_stations.dart"
      to: "lib/features/catalog/data/phase1_stations.dart"
      via: "Fallback streams are taken from the real stations, not duplicated as literals"
      pattern: "phase1Stations"
---

# Phase 1 Plan 05: The six real Bulgarian stations, owner-verified, with fallback streams

## Phase Goal

ROADMAP Phase 1 Goal (verbatim): "A user on a physical Android phone can start a Bulgarian station and it keeps playing on its own through screen-off, background, calls, headphone changes, network switches and dead primary streams. The live edge, correct Cyrillic now-playing and media controls all work." (This line is not in user-story form; see 01-01-PLAN.md "Phase Goal".)

<objective>
Replace the tracer's single station with the owner-verified six-station lineup (D-01). Each station carries its real alternate endpoints as ordered `streams[]` (D-04), together they cover the formats D-03 requires, and the debug-only test stations (D-05) let the owner exercise fallback rotation on a phone.

Purpose: Success Criterion 1 needs real data covering MP3, AAC+, HLS (including non-.m3u8), a .pls/.m3u wrapper, plain http:// and a windows-1251 title. PLAY-08 rotation needs real fallback streams. Every URL in RESEARCH is UNVERIFIED, because the station sites were blocked from the research environment, so D-02 requires the owner to confirm them first. That is a blocking decision.
Output: phase1_stations.dart (six stations), debug_stations.dart, the release/debug-aware StationDirectory, and data tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-playback-engine-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-playback-engine-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-playback-engine-walking-skeleton/01-PATTERNS.md

<interfaces>
Created by 01-01; read the real files:
- `lib/features/catalog/domain/station.dart`: `Station({required StationId id, required String name, required String nameLatin, required List<StationStream> streams, Uri? homepage})`, `StationStream({required Uri url, required StreamKind kind, String? codec, int? bitrateKbps, IcyCharset icyCharset = IcyCharset.auto})`, `StationId.curated(key)` / `StationId.debug(key)`
- `lib/features/catalog/data/station_directory.dart`: `StationDirectory.phase1()`, `all`, `byId`
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Owner verifies the six stations' official stream URLs and decides the three D-03 format gaps</name>
  <read_first>.planning/phases/01-playback-engine-walking-skeleton/01-RESEARCH.md (Station Stream Matrix lines 442-487, owner verification protocol 489-505, Assumptions A1-A4 lines 903-906, Open Questions 1-4 lines 919-927), .planning/phases/01-playback-engine-walking-skeleton/01-CONTEXT.md (D-01..D-05)</read_first>
  <decision>Confirm the stream table the release build will ship for the six Phase 1 stations, and choose how each of the three D-03 format gaps is covered.</decision>
  <context>
D-02 requires the owner to confirm that every URL plays and is the station's own official stream before any code relies on it. The researcher could not reach any station site (proxy-blocked), so every URL in 01-RESEARCH.md lines 446-466 is UNVERIFIED, and the БНР MP3 mount name is a guess (A2).

Run the protocol in 01-RESEARCH.md lines 489-505 on your Mac:
- DevTools capture from each station's own web player;
- `curl -s -D - -o /dev/null --max-time 6 -A 'eRadioto/0.1 (Android)' '<URL>'` for headers;
- `ffprobe` for codec and bitrate;
- the ICY byte probe for the charset, BG Radio first;
- a 30 s VLC play test.

Three gaps need a decision (D-03 forbids silently swapping stations):
1. No official Радио Витоша URL was found. Capture it from radiovitosha.com's player.
2. None of the six stations publishes HLS behind a non-.m3u8 URL.
3. Which station, if any, sends windows-1251 ICY titles is unknown until the byte probe runs.

Also noted for Phase 2, with no action now: the `dist=WEBSITEBG` tag on the Global-Audio URLs is the stations' own website tag. The Phase 2 catalogue validator must allow-list it per host (RESEARCH Open Question 3).

Current tracer fact: БГ Радио `http://play.global.audio/bgradio128` was confirmed or replaced at the 01-01 tracer gate. Keep a direct progressive stream as БГ Радио's primary.
  </context>
  <options>
    <option id="option-a">
      <name>Verified table + the proposed coverage for the gaps</name>
      <pros>No new sources needed. Non-.m3u8 HLS is proven by the resolver's sniff unit tests (01-04) plus a debug-only "Хоризонт (HLS sniff)" entry with kind unknown, which forces the content sniff on a real phone. windows-1251: every stream the probe proves to be cp1251 is marked icyCharset cp1251. If none is, the cp1251 golden tests in 01-06 cover it and SC1's cp1251 clause is shown by tests only.</pros>
      <cons>The non-.m3u8 case is exercised on a device only through a debug entry, not a real station URL. If no station sends cp1251, the device demo of that clause is missing.</cons>
    </option>
    <option id="option-b">
      <name>Verified table + another official endpoint of the same six stations for a gap</name>
      <pros>A real on-device case for non-.m3u8 HLS or cp1251, if one of the six stations publishes one elsewhere, for example a regional or alternate player endpoint.</pros>
      <cons>Needs more owner capture work. It must still be one of the six stations' own official endpoints (D-03).</cons>
    </option>
    <option id="option-c">
      <name>A station has no public official stream (for example Радио Витоша)</name>
      <pros>Honest: the station stays out of the Phase 1 release list and the gap is recorded in the plan SUMMARY and STATE, with no unofficial URL (content rights).</pros>
      <cons>Phase 1 ships five stations instead of six until an official stream is found (for example via Phase 2 curation).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The owner's reply contains a table row for every stream to ship: station, stream #, URL, kind (progressive/hls/pls/m3u/unknown), codec/bitrate, icyCharset (auto or cp1251 only when the probe proved it), source page, verifiedAt date, and VERIFIED or REJECTED.
    - The reply states the Радио Витоша outcome (a verified URL, or option-c), the non-.m3u8 HLS choice (a or b) and the cp1251 probe result (the station(s) found, or none).
  </acceptance_criteria>
  <resume-signal>Paste the filled stream table and choose option-a, option-b or option-c for each gap (Витоша, non-.m3u8 HLS, cp1251)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The six real stations and the debug-only test stations appear in the list with ordered fallback streams</name>
  <files>lib/features/catalog/data/phase1_stations.dart, lib/features/catalog/data/debug_stations.dart, lib/features/catalog/data/station_directory.dart, test/features/catalog/phase1_stations_test.dart, test/features/catalog/station_test.dart</files>
  <read_first>lib/features/catalog/data/phase1_stations.dart, lib/features/catalog/data/station_directory.dart, lib/features/catalog/domain/station.dart, test/app/tracer_e2e_test.dart, .planning/phases/01-playback-engine-walking-skeleton/01-RESEARCH.md (Station Stream Matrix 442-487, Pitfall I 554-555), .planning/phases/01-playback-engine-walking-skeleton/01-CONTEXT.md (D-01..D-05), the owner's Task 1 resume response</read_first>
  <behavior>
    - `StationDirectory.phase1(includeDebug: false).all` contains only curated: ids, all ids are unique, and every station has at least one stream.
    - The release list's streams collectively include at least one progressive stream with codec mp3, at least one with codec aac, at least one of kind hls, at least one of kind m3u or pls, and at least one http:// URL. If the owner's Task 1 decision removed a format (option-c), the test asserts the recorded reduced matrix and names the decision in its description.
    - `StationDirectory.phase1(includeDebug: true)` adds exactly the debug: ids. The dead-primary station's streams[0] host ends with .invalid and its streams[1] equals a real station's stream.
    - Only station_directory.dart imports debug_stations.dart.
    - `StationId.parse` round-trips every curated and debug id used here and throws FormatException for an unknown namespace, an empty key or a key with characters outside [a-z0-9-]. Constructing a `Station` with an empty streams list throws ArgumentError (the 01-01 assumption-delta invariant).
  </behavior>
  <action>
1. lib/features/catalog/data/phase1_stations.dart: replace the tracer list with the owner-verified stations (D-01), in this display order (national flagship first):

   | Display order | Id | name | nameLatin |
   |---|---|---|---|
   | 1 | curated:bnr-horizont | БНР Хоризонт | BNR Horizont |
   | 2 | curated:radio1 | Радио 1 | Radio 1 |
   | 3 | curated:bg-radio | БГ Радио | BG Radio |
   | 4 | curated:energy | Радио Енерджи | Radio Energy |
   | 5 | curated:njoy | N-JOY | N-JOY |
   | 6 | curated:vitosha | Радио Витоша | Radio Vitosha |

   Streams are in priority order (D-04), taken from the owner's Task 1 table. Ship only VERIFIED rows and omit REJECTED ones. Where the owner accepted the research proposal, use RESEARCH 446-462 with the "Recommended order" at 481-485:
   - Хоризонт: the lb-hls.cdn.bg playlist.m3u8 (hls), then the stream.bnr.bg:8011 AAC (progressive, aac), then the owner-confirmed 8011 MP3 mount (progressive, mp3).
   - Radio 1: the StreamTheWorld RADIO_1AAC_L.aac https redirect (progressive, aac), then radio1128 and radio164 (http mp3).
   - БГ Радио: bgradio128 (http mp3; primary, tracer-verified), then the StreamTheWorld BG_RADIOAAC_L.aac, then bgradio.aac.
   - Energy: the StreamTheWorld RADIO_ENERGYAAC_L.aac, then nrj128 and nrj64 (http mp3).
   - N-JOY: http://live.btvradio.bg/njoy.mp3.m3u (kind m3u, so STRM-03 runs on every N-JOY play), then http://live.btvradio.bg/njoy.mp3 (progressive mp3).
   - Радио Витоша: the owner-captured URL. If the owner chose option-c, omit the station and leave a comment citing the decision and date.
   Each StationStream sets codec, bitrateKbps and icyCharset. Use cp1251 only where the owner's probe proved it; otherwise auto. Above each stream put a one-line comment with its source page and the owner's verifiedAt date. Never add a station outside D-01 and never use an aggregator or relay URL (prohibition below).

2. lib/features/catalog/data/debug_stations.dart (D-05): define `final List<Station> debugStations` with StationId.debug ids:
   - `debug:dead-primary`, name "ТЕСТ: мъртъв основен поток": streams [https://dead-primary.invalid/stream.mp3 (progressive; RFC 6761 .invalid gives a fast NXDOMAIN), then Радио 1's first http MP3 stream];
   - `debug:slow-primary`, name "ТЕСТ: бавен основен поток": streams [http://192.0.2.1/stream.mp3 (progressive; RFC 5737 TEST-NET-1 is black-holed, exercising the connect-timeout path), then БГ Радио's streams[0]];
   - only if the owner chose option-a for non-.m3u8 HLS: `debug:horizont-hls-sniff`, name "ТЕСТ: Хоризонт (HLS sniff)": one stream with Хоризонт's HLS URL and kind unknown, so the resolver's content sniff must choose HLS.
   Read the fallback streams from phase1Stations by StationId instead of repeating URL literals.

3. lib/features/catalog/data/station_directory.dart: change the factory to `StationDirectory.phase1({bool includeDebug = !kReleaseMode})`, which appends debugStations only when includeDebug is true. kReleaseMode is a compile-time const, so AOT tree-shakes the debug list out of release builds (Pitfall I). No other file may import debug_stations.dart. Call sites (bootstrap and tests) keep calling `StationDirectory.phase1()`, so lib/app/bootstrap.dart is untouched.

4. test/features/catalog/phase1_stations_test.dart: the station-list behaviour cases above. test/features/catalog/station_test.dart: the StationId and empty-streams cases (01-01 created the check; this plan adds its test).
5. Run the full suite. test/app/tracer_e2e_test.dart must stay green: it finds БГ Радио by text with ensureVisible and reads the expected URL from the directory.
  </action>
  <verify>
    <automated>flutter test && flutter analyze && dart analyze</automated>
    <fails_when>non-zero exit, or "All tests passed!" missing from the flutter test output</fails_when>
    <automated>grep -q 'debug_stations.dart' lib/features/catalog/data/station_directory.dart && grep -q 'kReleaseMode' lib/features/catalog/data/station_directory.dart && ! grep -rl 'debug_stations.dart' lib | grep -v '^lib/features/catalog/data/station_directory.dart$' && ! grep -rl 'dead-primary.invalid' lib | grep -v '^lib/features/catalog/data/debug_stations.dart$' && grep -q "curated('bnr-horizont')" lib/features/catalog/data/phase1_stations.dart</automated>
    <fails_when>non-zero exit: the debug list is not gated by kReleaseMode, debug_stations.dart is imported outside the directory, the dead-primary host appears outside debug_stations.dart, or the flagship station is missing</fails_when>
    <human-check>
      <test>In a release build (CI APK from 01-02, or `flutter run --release`), play every station in the list for about 30 s each. In a debug build, confirm the ТЕСТ stations are listed; a release build must not list them.</test>
      <expected>Every release station plays within about 5 s (SC1): Хоризонт via HLS, N-JOY via its .m3u, at least one http:// station in release. The ТЕСТ entries appear only in debug/profile builds.</expected>
      <why_human>Only a phone on the owner's network can reach the real station servers, and only a release build proves the debug list is absent.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/catalog/station_test.dart` passes: StationId round trips and bad-input rejection, and a Station with an empty `streams` list is rejected.
    - `flutter test test/features/catalog/phase1_stations_test.dart` passes. It asserts: no debug: ids when includeDebug is false; unique ids; at least one stream per station; the D-03 coverage matrix (mp3, aac, hls, m3u|pls, http://), or the owner-recorded reduced matrix; and the debug ids and dead-primary shape when includeDebug is true.
    - Every stream literal in phase1_stations.dart has a source/verifiedAt comment from the owner's table, and no REJECTED URL is present.
    - debug_stations.dart is imported only by station_directory.dart, and `dead-primary.invalid` appears only in debug_stations.dart.
    - The full `flutter test` suite, including test/app/tracer_e2e_test.dart, passes.
  </acceptance_criteria>
  <done>The list shows the owner-verified stations with ordered official fallback streams; debug builds add the dead- and slow-primary test stations, and release builds do not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party stream hosts → app | The station data decides which servers the phone connects to |
| debug build → release build | Test-only stations must not cross into what users get |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / Spoofing (content rights) | phase1_stations.dart URLs | medium | mitigate | The D-02 blocking checkpoint: the owner verifies that each stream is the station's own official endpoint, and every stream carries a source/verifiedAt comment. No aggregator or relay URLs (prohibition). |
| T-05-02 | Information Disclosure | debug_stations.dart in release | medium | mitigate | A `!kReleaseMode` default parameter (tree-shaken), a unit test that the release list has no debug: ids, the import-locality grep gate, and the CI strings check on libapp.so (01-02). |
| T-05-03 | Tampering (MITM on http:// media) | http stations | low | accept | These are the stations' own official http endpoints. Where the station also offers https (StreamTheWorld), the https stream is listed first. Audio content only; no app data rides these connections. |
</threat_model>

<verification>
- `flutter test && flutter analyze && dart analyze` passes.
- The grep gates for the debug-list isolation pass.
- The owner's D-02 decision is recorded in the Task 1 resume and reflected in phase1_stations.dart.
</verification>

<success_criteria>
- SC1 data: the stations cover MP3, AAC+, HLS, a .pls/.m3u wrapper and plain http://, with the non-.m3u8 HLS and cp1251 cases covered as the owner decided.
- PLAY-08 data: real ordered fallback streams per station, plus debug dead-primary and slow-primary stations for rotation tests.
- D-05: the debug stations never ship in the release list.
</success_criteria>

## Edge Coverage (spec-less fallback — items owned by this plan)

| Req | Category | Status | Lifted as |
|-----|----------|--------|-----------|
| STRM-01 | unclassified | **unresolved — flagged assumption** | The probe assigned no edge category. Working assumption: MP3 and AAC/HE-AAC edges (SBR/PS decoding, StreamTheWorld 302 redirects to an edge host) are handled by ExoPlayer's extractors and `setAllowCrossProtocolRedirects(true)`. This plan only has to ship at least one of each codec, which the data test enforces. Whether a stream is HE-AAC or LC is confirmed with ffprobe at the D-02 checkpoint. Review manually. |

## Prohibitions (spec-less fallback — owned by this plan)

- MUST NOT ship any stream endpoint that is not the station's own official, publicly offered stream (no aggregator, relay or re-streamer URL), and MUST NOT silently substitute a different station or source to fill a missing format (D-03). Category values / content rights; status resolved; verification judgment. It is authored descriptor-less, so it stays flagged-unverified until review; it is never auto-dismissed.

## Artifacts this phase produces (this plan)

- Data: `phase1Stations` (curated:bnr-horizont, curated:radio1, curated:bg-radio, curated:energy, curated:njoy, curated:vitosha, the last subject to the owner's decision), `debugStations` (debug:dead-primary, debug:slow-primary, and debug:horizont-hls-sniff if option-a was chosen).
- API: `StationDirectory.phase1({bool includeDebug = !kReleaseMode})`.
- Tests: `test/features/catalog/phase1_stations_test.dart`, `test/features/catalog/station_test.dart`.

<output>
Create `.planning/phases/01-playback-engine-walking-skeleton/01-05-SUMMARY.md` when done. Record the owner's D-02 table, the gap decisions and verifiedAt dates in it.
</output>
